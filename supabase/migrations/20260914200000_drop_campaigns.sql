-- Заявка на кампанию снята с сайта, значит и таблица не нужна.
--
-- Форма обещала «we'll be in touch», а читать заявки было некому: уведомлений
-- нет, интерфейса нет, и узнать о новой строке можно было только открыв
-- дашборд. Обещание, которое некому исполнить, хуже отсутствующей формы.
--
-- Возвращать это стоит вместе с первыми продавцами: тогда кампания собирается
-- из живого инвентаря, а не руками. Таблица уходит пустой, терять нечего.

drop table if exists public.campaigns;
