/**
 * Лендинг на oxar.app. Один экран и четыре вещи на нём: метка, имя, строка и
 * дверь в приложение.
 *
 * Больше ничего нет намеренно. Объяснять продукт текстом, когда за кнопкой
 * стоит сама вещь с местами и ценами, значит пересказывать то, что человек
 * увидит через секунду. Вторая кнопка тут же сделала бы выбор из одного
 * действия выбором из двух.
 *
 * Серверного кода и состояния здесь нет, поэтому и "use client" не нужен.
 */

/** Само приложение живёт на своём домене и деплоится отдельным проектом. */
const APP_URL = "https://app.oxar.app";

export default function Home() {
  return (
    <main>
      {/* Метка растровая и мелкая - другой у нас нет. Поэтому она и стоит в
          44 пикселя при 84 в самом файле: на плотном экране это ровно два
          пикселя на точку, и она не мылится. */}
      <img className="mark" src="/mark.png" width={44} height={63} alt="" />
      <h1>OXAR</h1>
      <p className="lead">If people look at it, it&apos;s ad space.</p>
      <a className="launch" href={APP_URL}>
        See the auction
      </a>
    </main>
  );
}
