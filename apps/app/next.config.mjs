/** @type {import('next').NextConfig} */
const nextConfig = {
  // Статика: серверного кода нет, данные берутся из браузера. Так приложение
  // кладётся на Cloudflare Pages готовым каталогом и не тратит квоту сборок.
  output: "export",
  reactStrictMode: true,
  // Карты исходников в проде: без них стек ошибки в браузере минифицирован и
  // бесполезен. Репозиторий публичный, прятать код всё равно не от кого, а на
  // запуске цена немой ошибки выше лишних файлов в каталоге. Убрать, когда
  // первые торги пройдут и охота за багами кончится.
  productionBrowserSourceMaps: true,
  // Пакет воркспейса лежит в исходниках на TypeScript, его собирает Next.
  transpilePackages: ["@oxar/core"],
};

export default nextConfig;
