/**
 * Отпечаток сборки - короткий хэш коммита, из которого она собрана.
 *
 * По нему видно, докатился ли выкат: сравнил с хэшем смерженного PR и знаешь
 * наверняка, а не гадаешь. Подставляет его сборка (`NEXT_PUBLIC_BUILD_SHA` в
 * deploy.yml); локально переменной нет, и тогда «dev».
 *
 * Живёт отдельно, а не в компоненте: метку показывают в двух местах страницы
 * You - вошедшему и гостю, - и число должно быть одно.
 */
export const BUILD = (process.env.NEXT_PUBLIC_BUILD_SHA || "dev").slice(0, 7);
