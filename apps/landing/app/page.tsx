import { Desktop } from "./components/Desktop";

// Рабочий стол - клиентский компонент, но он проходит серверный рендер, поэтому
// текст первого открытого окна попадает в HTML. Остальные файлы индексируются
// как обычные страницы в /f/[slug] и открываются по прямой ссылке.

export default function Home() {
  return <Desktop />;
}
