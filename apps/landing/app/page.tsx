import { Desktop } from "./components/Desktop";

// Рабочий стол - клиентский компонент, но он проходит серверный рендер. Окон
// при первом заходе не открыто, поэтому в HTML этой страницы попадают только
// подписи иконок: сам оффер индексируется страницами /f/[slug], а описание
// сайта для превью ссылок берётся из метаданных в layout.tsx.

export default function Home() {
  return <Desktop />;
}
